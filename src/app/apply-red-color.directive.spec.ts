import { ApplyRedColorDirective } from './apply-red-color.directive';

describe('ApplyRedColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyRedColorDirective();
    expect(directive).toBeTruthy();
  });
});
